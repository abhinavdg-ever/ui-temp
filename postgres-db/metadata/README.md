# Deprecated — use `../manifest`

Manifest CSVs moved to **`postgres-db/manifest/`** (monorepo: `06-postgres-db/manifest`).

The app and insert script still fall back to this folder if `manifest/` is missing.
